# H-011 publisher validation trigger

This file exists only to create an isolated pull-request event against the audit branch so GitHub Actions can execute the hostile publisher crash matrix from a workflow already present in the PR base.

It contains no product code, data, secrets, deployment configuration, or runtime behavior.
